<?php
class Layout
{
    //the the codes which used to make generic layout of html from data is put here.
    public static function GetLeftNavHtml($navLinks)
    {
        foreach($navLinks as $linkTitle=>$aLink)
        {
            if(is_array($aLink))
            {
                
            }
        }
    }
}
?>